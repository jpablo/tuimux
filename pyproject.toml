[project]
name = "tuimux"
version = "0.1.0"
description = "A Textual TUI for tmux."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "textual>=0.50,<1.0",
]

[project.scripts]
tuimux = "tuimux.app:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
